# Contributing

## Workflow
1. Create a feature branch.
2. Run `just build` and `bash tests/smoke.sh`.
3. Submit PR with architecture notes.

## Standards
- TypeScript strict mode for JS services.
- Rust stable toolchain for microservices.
- Event contracts documented in `api/openapi.yaml` and `docs/API.md`.
